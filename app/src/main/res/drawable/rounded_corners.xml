<!-- res/drawable/rounded_corners.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Màu nền -->
    <corners android:radius="16dp"/> <!-- Độ bo góc -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray"/> <!-- Đường viền -->
</shape>
